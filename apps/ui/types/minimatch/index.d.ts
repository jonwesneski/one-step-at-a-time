declare module 'minimatch' {
  // Minimal stub to satisfy TypeScript when a type-library called "minimatch" is implicitly requested.
  // Replace with fuller typings if needed later.
  const minimatch: any;
  export = minimatch;
}
